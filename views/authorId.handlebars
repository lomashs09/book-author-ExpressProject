<h1>{{title}}</h1>

<table class="table mt-4 table table-striped">
    <thead>
        <tr>
            <th scope="col">Author</th>
        </tr>
    </thead>
    <tbody>
        {{#each results}}
        <tr>
            <td>{{this.name}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>